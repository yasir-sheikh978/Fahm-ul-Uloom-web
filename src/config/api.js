// Base URL of the Fahm-ul-Uloom API (the server/ folder).
// Locally: http://localhost:5000 — in production set VITE_API_URL in your .env
export const API_BASE = import.meta.env.VITE_API_URL || "http://localhost:5000";
